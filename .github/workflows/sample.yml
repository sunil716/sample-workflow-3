name : First Workflow

on : 
  push :
    branches :
      - main

jobs :
  run-shell-commands : 
    runs-on : ubuntu-latest
    
    steps : 
      - name : first-command
        run : echo "First command"
      - name : second-cmd
        run : |
          ls -al
          echo "Hello"
